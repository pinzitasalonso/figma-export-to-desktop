{
  "name": "figma-export-to-desktop",
  "version": "1.0.0",
  "description": "Figma plugin — export frames as PNG directly to a folder on your Desktop",
  "scripts": {
    "build:ui": "node scripts/build-ui.js",
    "build:ts": "tsc -p tsconfig.json",
    "build": "npm run build:ui && npm run build:ts",
    "watch": "npm run build:ui && tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "typescript": "^5.0.0"
  }
}
